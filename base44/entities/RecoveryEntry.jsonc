{
  "name": "RecoveryEntry",
  "properties": {
    "data": {
      "type": "object"
    },
    "date": {
      "format": "date",
      "type": "string"
    },
    "entry_time": {
      "type": "string"
    },
    "note": {
      "type": "string"
    },
    "patient_id": {
      "description": "Patient this entry belongs to.",
      "type": "string"
    },
    "surgery_id": {
      "description": "Surgery this entry is being tracked against.",
      "type": "string"
    },
    "type": {
      "enum": [
        "checkin",
        "water",
        "food",
        "med",
        "pain",
        "temp",
        "urine",
        "bm",
        "pads",
        "incisions",
        "garment",
        "skin",
        "walk",
        "movement",
        "sleep",
        "mld",
        "photo",
        "measure",
        "weight"
      ],
      "type": "string"
    }
  },
  "required": [
    "date",
    "type",
    "entry_time"
  ],
  "type": "object",
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    }
  }
}