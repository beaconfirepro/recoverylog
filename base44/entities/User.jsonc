{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "patient_id": {
      "type": "string",
      "description": "Patient whose log this user may read and write. Set when a care-team invite is claimed; blank until then."
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}
