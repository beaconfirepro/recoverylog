{
  "name": "SurgeryInfo",
  "type": "object",
  "properties": {
    "patient_id": {
      "type": "string",
      "description": "Patient this surgery record belongs to."
    },
    "surgery_date": {
      "type": "string",
      "format": "date"
    },
    "surgery_time": {
      "type": "string"
    },
    "procedure": {
      "type": "string"
    },
    "surgeon": {
      "type": "string"
    },
    "office_phone": {
      "type": "string"
    },
    "fever_threshold": {
      "type": "number"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.patient_id": "{{user.data.patient_id}}"
        }
      ]
    }
  }
}
